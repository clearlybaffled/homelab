apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: jellyfin
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
  - chart: app-template
    repoURL: https://bjw-s.github.io/helm-charts/
    targetRevision: 2.4.0
    helm:
      valueFiles:
      - $repo/cluster/apps/media/jellyfin/values.yaml
      releaseName: jellyfin
  - repoURL: https://github.com/clearlybaffled/homelab
    path: cluster/apps/media/jellyfin
    targetRevision: main
    ref: repo
  destination:
    name: in-cluster
    namespace: media
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    - ServerSideApply=true
