# BEGIN ANSIBLE MANAGED BLOCK
#
# template: infrastructure/roles/cluster/templates/application.yaml.j2
# date: 2023-07-29 00:26:25
# user: clearlybaffled
# host: tirante
# commit: b586499
#
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rook-ceph
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  project: default
  
  sources:
    - repoURL: https://github.com/clearlybaffled/homelab
      path: cluster/apps/infrastructure/rook-ceph
      targetRevision: development
      ref: repo

    - repoURL: https://charts.rook.io/release
      chart: rook-ceph
      targetRevision: v1.11.7
      helm:
        valueFiles:
          - $repo/cluster/apps/infrastructure/rook-ceph/operator.yaml

    - repoURL: https://charts.rook.io/release
      chart: rook-ceph-cluster
      targetRevision: v1.11.7
      helm:
        releaseName: rook-ceph-cluster
        valueFiles:
          - $repo/cluster/apps/infrastructure/rook-ceph/ceph-cluster.yaml

  destination:
    name: in-cluster
    namespace: rook-ceph

  syncPolicy: 
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
# END ANSIBLE MANAGED BLOCK
