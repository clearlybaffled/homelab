# BEGIN ANSIBLE MANAGED BLOCK
#
# template: infrastructure/roles/cluster/templates/application.yaml.j2
# date: 2023-07-18 03:38:25
# user: clearlybaffled
# host: tirante
# commit: 7d91d84
#
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hajimari
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  project: default
  
  sources:
    - chart: hajimari
      repoURL: https://hajimari.io
      targetRevision: 2.0.2
      helm:
        valueFiles:
          - $repo/cluster/apps/home/hajimari/values.yaml

    - repoURL: ssh://git@parche:/srv/homelab.git
      path: cluster/apps/home/hajimari
      targetRevision: parche
      ref: repo

  destination:
    name: in-cluster
    namespace: home

  syncPolicy: 
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
# END ANSIBLE MANAGED BLOCK
