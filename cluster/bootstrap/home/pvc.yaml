apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-files
  namespace: home
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5.5Ti
  volumeName: shared-files-home
  storageClassName: local-storage
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: videos
  namespace: home
spec:
  resources:
    requests:
      storage: 7.5Ti
  accessModes:
    - ReadWriteMany
  volumeName: videos-home
  storageClassName: local-storage
