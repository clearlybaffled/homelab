apiVersion: v1
kind: Service
metadata:
  name: cloudnative-pg
  namespace: db
spec:
  selector:
    cnpg.io/cluster: cnpg-cluster
    role: primary
  ports:
  - name: postgres
    protocol: TCP
    port: 5432
    targetPort: 5432
  type: LoadBalancer
  externalTrafficPolicy: Cluster
