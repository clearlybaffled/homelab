clusterDomain: seawolf
auth:
  enabled: false

master:
  persistence:
    enabled: false
  service:
    type: LoadBalancer
  dnsConfig:
    options:
      - name: ndots
        value: "8"

replica:
  persistence:
    enabled: false
  dnsConfig:
    options:
      - name: ndots
        value: "8"

metrics:
  enabled: true
  serviceMonitor:
    enabled: true
    interval: 1m
useExternalDNS:
  enabled: true
