hajirmari:
  defaultEnable: true
  alwaysTargetBlank: true
  name: Jared
ingress:
  main:
    enabled: true
    hosts:
      - host: hajimari.hajimari.svc.gato.hermleigh.home
        paths:
          - path: /
            pathType: Prefix
persistence:
  data:
    enabled: false
    accessMode: ReadWriteOnce
    size: 1Gi

