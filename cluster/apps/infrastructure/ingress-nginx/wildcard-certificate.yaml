apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard
  namespace: infrastructure
spec:
  secretName: wildcard-certificate
  issuerRef:
    kind: ClusterIssuer
    name: kube-ca-issuer
  commonName: "seawolf.hermleigh.home"
  subject:
    organizations:
      - "HERMLEIGH.HOME"
    organizationalUnits:
      - "Kubernetes Cluster - Gato"
    countries:
      - "US"
  dnsNames:
    - "seawolf.hermleigh.home"
    - "seawolf"
    - "*.seawolf.hermleigh.home"
    - "*.seawolf"
  ipAddresses:
    - "172.16.1.24"
