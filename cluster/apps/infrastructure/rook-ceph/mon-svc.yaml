---
apiVersion: v1
kind: Service
metadata:
  name: rook-ceph-mon-ext
  namespace: rook-ceph
  annotations:
    metallb.universe.tf/loadBalancerIPs: 172.16.1.34
    external-dns.alpha.kubernetes.io/hostname: ceph.hermleigh.home
  labels:
    app: rook-ceph-mon
    app.kubernetes.io/component: cephclusters.ceph.rook.io
    rook.io/operator-namespace: rook-ceph
    rook_cluster: rook-ceph
    app.kubernetes.io/name: ceph-mon
    app.kubernetes.io/part-of: rook-ceph
spec:
  ports:
  - name: tcp-msgr1
    port: 6789
    protocol: TCP
    targetPort: 6789
  - name: tcp-msgr2
    port: 3300
    protocol: TCP
    targetPort: 3300
  selector:
    app: rook-ceph-mon
    mon_cluster: rook-ceph
    rook_cluster: rook-ceph
  sessionAffinity: None
  type: LoadBalancer
