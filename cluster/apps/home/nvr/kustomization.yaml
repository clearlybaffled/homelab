#
# Ansible managed
#
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: home
configMapGenerator:
- name: frigate-configmap
  files:
  - ./config.yaml
  options:
    disableNameSuffixHash: true
resources:
- ./storage.yaml
