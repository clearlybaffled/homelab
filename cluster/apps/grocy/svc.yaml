apiVersion: v1
kind: Service
metadata:
  name: grocy-svc
  labels:
    app: grocy
spec:
  selector:
    app: grocy
  ports:
    - name: web
      port: 80
      targetPort: 80
      protocol: TCP
  loadBalancerClass: metallb
  type: LoadBalancer
