apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grocy
  namespace: services
  annotations:
    hajimari.io/icon: "cart-variant"
    hajimari.io/appName: "Grocy"
spec:
  ingressClassName: nginx
  rules:
  - host: grocy.seawolf.hermleigh.home
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: grocy
            port:
              number: 80
  tls:
    - hosts:
      - grocy.seawolf.hermleigh.home
      secretName: grocy-ingress-cert
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: barcode-buddy
  namespace: services
  annotations:
    hajimari.io/icon: ph:barcode
spec:
  ingressClassName: nginx
  rules:
  - host: barcode-buddy.seawolf.hermleigh.home
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: barcode-buddy
            port:
              number: 80
  tls:
    - hosts:
      - barcode-buddy.seawolf.hermleigh.home
      secretName: barcode-buddy-ingress-cert
