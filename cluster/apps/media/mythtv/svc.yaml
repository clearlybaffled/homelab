apiVersion: v1
kind: Service
metadata:
  name: mythtv-svc
  labels:
    app: mythtv
  namespace: mythtv
spec:
  selector:
    app: mythtv
  ports:
    - name: web
      port: 6443
      targetPort: 80
      protocol: TCP
  type: ClusterIP
