# BEGIN ANSIBLE MANAGED BLOCK
#
# template: infrastructure/roles/cluster/templates/application.yaml.j2
# date: 2023-08-27 23:40:33
# user: clearlybaffled
# host: tirante
# commit: b586499
#
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: listen-brainz
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
spec:
  project: default
  
  source:
    repoURL: https://github.com/clearlybaffled/homelab
    path: cluster/apps/media/listen-brainz
    targetRevision: development

  destination:
    name: in-cluster
    namespace: media

  syncPolicy: 
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
# END ANSIBLE MANAGED BLOCK
