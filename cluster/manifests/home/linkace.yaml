# BEGIN ANSIBLE MANAGED BLOCK
#
# template: infrastructure/roles/cluster/templates/application.yaml.j2
# date: 2023-09-08 11:04:38
# user: clearlybaffled
# host: tirante
# commit: 37cbe6a
#
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: linkace
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
spec:
  project: default
  
  sources:
    - chart: app-template
      repoURL: https://bjw-s.github.io/helm-charts/
      targetRevision: 1.5.1
      helm:
        releaseName: linkace
        valueFiles:
          - $repo/cluster/apps/home/linkace/values.yaml

    - repoURL: https://github.com/clearlybaffled/homelab
      path: cluster/apps/home/linkace
      targetRevision: main
      ref: repo

  destination:
    name: in-cluster
    namespace: home

  syncPolicy: 
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
# END ANSIBLE MANAGED BLOCK
