# BEGIN ANSIBLE MANAGED BLOCK
#
# template: infrastructure/roles/cluster/templates/application.yaml.j2
# date: 2023-08-27 23:40:33
# user: clearlybaffled
# host: tirante
# commit: b586499
#
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mealie
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  project: default
  
  sources:
    - chart: mealie
      repoURL: https://k8s-at-home.com/charts/
      targetRevision: 5.1.2
      helm:
        valueFiles:
          - $repo/cluster/apps/home/mealie/values.yaml

    - repoURL: https://github.com/clearlybaffled/homelab
      path: cluster/apps/home/mealie
      targetRevision: development
      ref: repo

  destination:
    name: in-cluster
    namespace: home

  syncPolicy: 
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
# END ANSIBLE MANAGED BLOCK
