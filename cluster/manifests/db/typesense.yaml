# BEGIN ANSIBLE MANAGED BLOCK
#
# template: infrastructure/roles/cluster/templates/application.yaml.j2
# date: 2023-08-27 23:40:33
# user: clearlybaffled
# host: tirante
# commit: b586499
#
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: typesense
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
spec:
  project: default
  
  sources:
    - chart: app-template
      repoURL: https://bjw-s.github.io/helm-charts/
      targetRevision: 1.5.1
      helm:
        releaseName: typesense
        valueFiles:
          - $repo/cluster/apps/db/typesense/values.yaml

    - repoURL: https://github.com/clearlybaffled/homelab
      path: cluster/apps/db/typesense
      targetRevision: development
      ref: repo

  destination:
    name: in-cluster
    namespace: db

  syncPolicy: 
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
# END ANSIBLE MANAGED BLOCK
