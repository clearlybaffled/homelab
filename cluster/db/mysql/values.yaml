metrics:
  enabled: true
auth:
  createDatabase: false
primary:
  persistence:
    storageclass: local-path
  podSecurityContext:
    enabled: true
    fsGroup: 997
  containerSecurityContext:
    enabled: true
    runAsUser: 997
    runAsNonRoot: true
  service:
    type: ClusterIP
