metrics:
  enabled: true
auth:
  createDatabase: false
primary:
  persistence:
    #    storageclass: local-path
    existingClaim: data-mysql-0
  podSecurityContext:
    enabled: true
    fsGroup: 997
  containerSecurityContext:
    enabled: true
    runAsUser: 997
    runAsNonRoot: true
  service:
    type: ClusterIP
