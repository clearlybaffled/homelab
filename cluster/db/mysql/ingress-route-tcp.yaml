apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: mysql
  namespace: db
  labels:
    name: mysql-ingress
spec:
  entryPoints:
    - mysql
  routes:
    - match: HostSNI(`*`)
      services:
        - name: mysql
          port: 3306
