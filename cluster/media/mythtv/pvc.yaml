---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mythtv-homedir
  namespace: mythtv
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
  storageClassName: local-path
