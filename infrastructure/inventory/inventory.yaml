all:
  hosts:
    parche:
      ip: 172.16.1.10
    barb:
      ip: &barb_ip 172.16.1.186
    growler:
      ip: 172.16.1.1
    tirante:
      ansible_host: *barb_ip
      ansible_port: 2222
    seawolf:
      ip: 172.16.1.11
      ansible_host: parche
    localhost:
      ansible_connection: local
  vars:
    domain_name: hermleigh.home
    ansible_user: ansible
    ansible_ssh_private_key_file: ~/.ssh/ansible

kube_control_plane:
  hosts:
    seawolf:

kube_node:
  hosts:
    seawolf:

kubectl:
  hosts:
    seawolf:
    localhost:

k8s_cluster:
  children:
    kube_control_plane:
    kube_node:

servers:
  hosts:
    parche:
    # growler:

workstations:
  hosts:
    # barb:
    localhost:

hypervisor:
  hosts:
    parche:
