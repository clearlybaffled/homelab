---
- name: Flannel | Start Resources
  kubernetes.core.k8s:
    template: cni-flannel.yml.j2
    state: present

- name: Flannel | Wait for flannel subnet.env file presence
  delegate_to: "{{ groups['kube_control_plane'][0] }}"
  wait_for:
    path: /run/flannel/subnet.env
    delay: 5
    timeout: 600
