---
# - name: paperless-ngx | Create postgres resources
#   delegate_to: "{{ lookup('random_choice', hosts['kube_control_plane']) }}"
#   block:
#     - name: Create user
#       community.postgresql.postgresql_user:
#         name: paperless
#         password: "{{ db_pass }}"
#         state: present
#         login_user: "{{ postgres_admin.username | b64decode }}"
#         login_password: "{{ postgres_admin.password | b64decode }}"
#         host: cnpg-cluster-rw.db

#     - name: Create database
#       community.postgresql.postgresql_db:
#         name: paperless
#         owner: paperless
#         state: present
#         login_user: "{{ postgres_admin.username | b64decode }}"
#         login_password: "{{ postgres_admin.password | b64decode }}"
#         host: cnpg-cluster-rw.db
