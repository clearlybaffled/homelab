---
- name: Provision Server
  tags: server
  ansible.builtin.import_playbook: host.yml

- name: Install CRI-O as container engine
  hosts: kube_control_plane
  gather_facts: true
  any_errors_fatal: true
  roles:
    - container_engine
