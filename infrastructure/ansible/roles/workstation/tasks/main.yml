---
# ansible.windows.win_get_url:

- name: define packages
  vars:
    packages:
      - obs
      - vscode
      - google-drive
      - acrobat
      - npp
      - office
      - bcompare
      - adobe-creative-cloud
      - quicken
      - keepass
