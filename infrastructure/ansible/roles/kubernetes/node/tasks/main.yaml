---
- name: Install k8s system packages
  ansible.builtin.package:
    name: "{{ k8s_common_packages | union(k8s_required_packages|default([])) }}"
    state: present

- name: Load os specific tasks
  ansible.builtin.import_tasks:
    file: "{{ ansible_os_family | lower }}"
